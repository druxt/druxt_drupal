name: Druxt.js
description: Provides an easy connection from Drupal to a Druxt.js frontend using Drupal's JSON API.

type: module
core_version_requirement: ^8.8 || ^9

dependencies:
  - drupal:decoupled_router
  - drupal:jsonapi
